# BtReceiverPy
Receives, logs, parses data from Android app "SensorLogger"
btrec.py contains a basic class which handles communication from an Android phone with the SensorLogger app;
slparse.py contains a wip parser which transforms the raw txt logs from said app into two distinct accelerometer and gps Pandas Dataframes. 

